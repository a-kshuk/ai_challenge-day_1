# 📚 Simple GigaChat Agent

Это простое веб-приложение, демонстрирующее взаимодействие с моделью GigaChat через собственный API. Оно позволяет пользователям задавать вопросы и получать ответы от AI-модели.

## 💡 Описание

Проект состоит из двух частей:

- **Backend** на Express.js, который принимает запросы и обращается к API GigaChat.
- **Frontend**, выполненный на чистом HTML/CSS/JS, позволяющий удобно вводить запросы и получать ответы.

Приложение предназначено для демонстрации возможностей интеграции моделей искусственного интеллекта в веб-проекты.

## 🛠️ Требования

Перед началом убедитесь, что у вас установлены:

- Node.js версии >= 14.x
- NPM или YARN
- Доступ к API GigaChat (токен API)

## 🔧 Установка и настройка

1. **Клонируйте проект:**

```bash
git clone https://github.com/a-kshuk/ai_challenge-day_1.git
cd ai_challenge-day_1
```

2. **Установите зависимости:**

```bash
npm install
```

3. **Настроить токен API:**

Создайте файл `.env` в корневой директории проекта и добавьте свой токен API:

```
GIGACHAT_API_TOKEN=your_secret_api_token_here
```

4. **Запустить сервер:**

```bash
npm start
```

Открывайте браузер по адресу `http://localhost:3000`, чтобы увидеть демонстрационную страницу и начать общение с GigaChat.

## 👨‍💻 Использование

1. Напишите свой вопрос в поле ввода и нажмите кнопку "Отправить".
2. Посмотрите ответ, предоставляемый моделью GigaChat.

## 🎯 Архитектура

Проект организован следующим образом:

- **`./server/app.js`**: Основной серверный скрипт на Express.js, обрабатывающий запросы и взаимодействующий с API GigaChat.
- **`./public/index.html`**: Главная HTML-страница с формой для ввода вопросов.
- **`./public/script.js`**: Клиентская логика на JavaScript, связанная с отправкой запросов и обработкой ответов.
- **`./public/style.css`**: Базовые стили оформления интерфейса.
